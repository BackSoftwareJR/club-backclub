# Club CRM — production build for club.backclub.it
# Copy before build: cp .env.production.example .env.production
# Vite loads .env.production automatically when NODE_ENV=production (npm run build)

VITE_API_URL=https://club.backclub.it/api
